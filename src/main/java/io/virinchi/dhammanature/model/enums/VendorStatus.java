package io.virinchi.dhammanature.model.enums;

/** FR-07 / Business Rule 3: a vendor application is pending, verified, or rejected. */
public enum VendorStatus {
    PENDING,
    VERIFIED,
    REJECTED
}